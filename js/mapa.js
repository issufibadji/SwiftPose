export function renderHeatmap(results, img, scaledWidth, scaledHeight, containerId) {
    const container = document.getElementById(containerId);
    container.innerHTML = ''; // Limpa o contêiner antes de adicionar novos elementos

    // Implementação do mapa de calor (você pode usar D3.js ou outra biblioteca)
    // Aqui, o código do mapa de calor vai dentro desse container
}